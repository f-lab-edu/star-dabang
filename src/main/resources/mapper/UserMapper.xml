<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dabang.star.cafe.infrastructure.mybatis.mapper.UserMapper">
    <insert id="insert">
        insert into users (id, email, password, nickname, tel, birthday)
        values (#{user.id},
                #{user.email},
                #{user.password},
                #{user.nickname},
                #{user.tel},
                #{user.birthday})
    </insert>

    <select id="findById" resultMap="user">
        select id, email, password, nickname, tel, birthday
        from users
        where id = #{id}
    </select>

    <select id="findByEmail" resultMap="user">
        select id, email, password, nickname, tel, birthday
        from users
        where email = #{email}
    </select>

    <update id="update">
        update users
        <set>
            <if test="user.email !=''">email=#{user.email},</if>
            <if test="user.password !=''">password = #{user.password},</if>
            <if test="user.nickname !=''">nickname = #{user.nickname},</if>
            <if test="user.tel !=''">tel = #{user.tel},</if>
            <if test="user.birthday != null">birthday = #{user.birthday}</if>
        </set>
        where id = #{user.id}
    </update>


    <resultMap id="user" type="dabang.star.cafe.domain.user.User">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="tel" property="tel"/>
        <result column="birthday" property="birthday"/>
    </resultMap>
</mapper>