<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dabang.star.cafe.infrastructure.mapper.read.MemberReadService">

    <select id="exist" resultType="boolean">
        SELECT EXISTS(SELECT m.member_id FROM members m WHERE m.email = #{email})
    </select>

    <select id="findByEmail" resultMap="memberResultMap">
        SELECT * FROM members m WHERE m.email = #{email}
    </select>

    <resultMap id="memberResultMap" type="Member">
        <id property="id" column="member_id" />
        <result property="email" column="email"/>
        <result property="password" column="passwd"/>
        <result property="nickname" column="nickname"/>
        <result property="telephone" column="telephone"/>
        <result property="birth" column="birth"/>
    </resultMap>
</mapper>