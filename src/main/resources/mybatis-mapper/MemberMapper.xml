<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dabang.star.cafe.mapper.MemberMapper">
    <insert id="save">
        insert into member (email, passwd, nickname, phone, birth) values (#{email}, #{passwd}, #{nickname}, #{telephone}, #{birth})
    </insert>
</mapper>